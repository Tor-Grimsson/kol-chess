---
_template:
  version: 1
  path: .kol/llm-plan/01-parking-lot.md
  sync: skip
---

# kol-chess — future exploration

Ideas that aren't committed work. Move items out of here when they become real roadmap entries in `llm-context/AGENT-CONTEXT.md`.

Delete this file entirely if you have no speculative work yet — create it later when exploring a direction you're not committed to.

---

## App shell / nav

kol-chess has no shell — the one view is the whole app. `@kolkrabbi/kol-framework@0.5.0` (already a dependency for ThemeToggle) ships SideNav, layout, footer, heroes. When a second view exists (e.g. the stats dashboard in [[02-engine-analysis|the engine plan]] "Later"), mount the shell then — not before.

## [First speculative item]

<!-- Structure each item as:
## [name]
[One-paragraph premise: what this would be, what problem it solves.]

### shape
[What the interface or user-facing surface looks like]

### architecture
[Rough technical breakdown — components, flow]

### trade-offs
[What this gives up vs current approach]

### open questions
[Unknowns that need answering before this becomes real work]

### kill criteria
[Conditions that would mean abandoning this path]
-->

---

Nothing here is committed. This doc is a thought exercise until items graduate to `llm-context/AGENT-CONTEXT.md`.

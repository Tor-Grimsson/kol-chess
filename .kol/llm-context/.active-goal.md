status: done
iter: 0
max: 30
goal: Consumer switch onto the one view (6 steps): extract EnginePanel+OpeningStrip to src/engine/AnalysisPanel.jsx; mount on home via ChessAnalysisLayout panel slot + controlled externalGame; paste flow (reuse resolveGameInput) via overlayActions in the Games overlay; add @kolkrabbi/kol-framework and mount ThemeToggle; delete /analyse route + src/pages/Analyse.jsx (keep engine/openings/resolveGame modules + tests); verify with tests + build, sync plan file + AGENT-CONTEXT + playbook.

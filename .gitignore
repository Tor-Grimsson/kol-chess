# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Obsidian per-vault local state (docs vault config is symlinked from dotfiles)
docs/.obsidian/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent boot symlink (scaffold-llm-context)
LLM_RULES.md
_tmp/

# ephemeral: /kol-goal loop state (session-keyed)
.kol/llm-context/.active-goal-*.md

# Python training stack (train/) — the venv and the generated tensors.
# The SCRIPTS are tracked; the environment and the multi-hundred-MB dataset
# they produce are not.
train/.venv39/
train/__pycache__/
train/data/
